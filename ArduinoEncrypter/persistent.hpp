#ifndef PERSISTENT
#define PERSISTENT
namespace	persistent
{
  constexpr auto PrivateKey = 0;
  constexpr auto PublicKey = 32;
  constexpr auto ServerKey = 64;
  constexpr auto NextFreeEEPROM = 92;
}
#endif